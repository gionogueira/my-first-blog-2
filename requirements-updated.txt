# Requirements atualizados para melhor segurança e performance
Django>=5.0,<5.1
asgiref>=3.7.0,<4.0.0
sqlparse>=0.4.4
tzdata>=2023.3

# Dependências para desenvolvimento (opcional)
# python-decouple==3.8  # Para gerenciar variáveis de ambiente
# pillow>=10.0.0  # Para uploads de imagem (futuro)
# django-crispy-forms>=2.0  # Para forms mais bonitos (futuro)
